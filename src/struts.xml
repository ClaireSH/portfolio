<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
	<!--요청에 대한   -->

    <constant name="struts.enable.DynamicMethodInvocation" value="false" />
    <constant name="struts.devMode" value="true" />

	
    <package name="default" namespace="/" extends="struts-default">
    	<interceptors>
        	<!-- 로그인 인터셉터 정의 -->
            <interceptor name="loginInterceptor" class="interceptor.LoginInterceptor" />
        	
			 <interceptor-stack name="loginInterceptorStack">
				<interceptor-ref name="defaultStack" />
				<interceptor-ref name="loginInterceptor" />
			</interceptor-stack>
		</interceptors>
		<default-action-ref name="index" />
    	<!-- 
    	<global-results>
	        모든 액션에서 "login"이 리턴되었을 때의 경로 (LoginInterceptor에서 리턴함)
  			
  		</global-results>
  		 -->
  		 
  		<!-- 인덱스로 이동 -->
		<action name="index">
			<result>/index.html</result>
		</action>
		
    	
    	<action name="main" class="action.Action" method="login">
    		<result name="login" type="redirect">/pages/main.html</result>
    	</action>
    	
    	<action name="join" class="action.Action" method="join">
    		<!--회원가입 성공  -->
    		<!--Chain을 하면 Action클래스의 "ValueObject"가 잔존하여 다음 액션(main)에도 값을 줄 수 있음 -->
    		<result name="success" type="chain">main</result>
    		<!--회원가입 실패  -->
    		<result name="input" type="redirect">/index.action</result>
    	</action>
		
		
		<!-- <action name="favoriteMemberPage">
    		<result>/favoriteMemberPage.jsp</result>
    	</action> -->
    	
    	
    	
    	
    	
    	
    </package>

    <!-- Add packages here -->
  
    
    
</struts>
